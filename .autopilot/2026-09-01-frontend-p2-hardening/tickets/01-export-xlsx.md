# 01 — Экспорт XLSX для админа

**Требования:** R01
**Blocked by:** —
**Зона:** `src/features/registry/export/`
**Волна:** 1
**Status:** ready

## Что должно заработать
Кнопка «Экспорт XLSX» в реестрах (видна только cntr_admin) скачивает XLSX с текущими фильтрами (те же строки что на экране, limit 20 или все отфильтрованные). Client-side exceljs, без бэк-изменений, escape формул.

## Из брифа
> «XLSX админу»

## Разделы спецификации
Истории 1, Решения § export, Шов 1

## Критерии приёмки
- [ ] Кнопка Экспорт видна только cntr_admin (cntr_manager скрыта), жмёт → XLSX скачивается, содержит заголовки + строки текущей выборки
- [ ] Фильтры из URL применяются к экспорту, escape `=+-@` в ячейках
- [ ] exceljs через `npm i exceljs` или BLOCKED если нет, `npm test` 58+ зелёно, build ok
