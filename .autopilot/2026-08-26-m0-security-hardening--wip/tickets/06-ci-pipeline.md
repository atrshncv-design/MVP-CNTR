# 06 — CI-конвейер GitHub Actions

**Требования:** R04, R17i
**Blocked by:** 01 (mypy доводится до зелёного поверх финального кода бэкенда)
**Зона:** `.github/workflows/` + точечные правки типов по всему `technozrelost-backend/app/` (только аннотации)
**Волна:** 2
**Status:** ready

## Что должно заработать

Каждый push и каждый pull request прогоняет оба приложения через полный набор проверок:
бэкенд — тесты на настоящей БД с pgvector, линтер, строгий типизатор; фронтенд — линтер,
тесты и production-сборку. Красный конвейер виден до того, как дефект увидит владелец.
«mypy strict объявлен» становится доказанным фактом.

## Из брифа, дословно

> «CI-пайплайн (P-12)»

## Разделы спецификации

История 7; Решения §CI; таблица соответствия (P-12).

## Критерии приёмки

- [ ] Workflow: job бэкенда (сервис-контейнер PostgreSQL с pgvector, установка зависимостей с dev-экстрами, ruff, mypy, pytest) + job фронтенда (установка, lint, node --test, next build); триггеры push и pull_request
- [ ] Конвейер зелёный на ветке прогона (реальный прогон, не «должно сработать»)
- [ ] mypy по всему бэкенду доведён до зелёного; если объём требуемых правок выходит за пределы аннотаций (>50 изменённых строк логики) — остановиться и отчитаться, а не чинить молча
- [ ] Секреты конвейеру не нужны; кэширование зависимостей настроено, время пайплайна разумное
- [ ] Правки типов не меняют поведение (тесты остаются зелёными без других правок)
- [ ] BACKLOG.md P-12 → `done` с хешем
