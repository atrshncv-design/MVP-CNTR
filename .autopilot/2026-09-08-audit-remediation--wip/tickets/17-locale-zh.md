# 17 — Локализация интерфейса на китайский

**Требования:** G01 (история 22)
**Blocked by:** —
**Зона:** `frontend/i18n`
**Волна:** 4
**Status:** ready

## Что должно заработать

Интерфейс переключается на китайский: словарь zh покрывает 100% ключей EN
во всех неймспейсах и обоих зеркалах словарей; локаль доступна через
переключатель; недостающий ключ даёт fallback, а не пустое место.

## Из брифа, дословно

> добавь ещё 2 таска: по аналогии с переводом интерфейса платформы на английский язык нужно будет сделать ещё китайский и хинди
> Но текущий локал хост пока не трогай (Дополнения 2026-09-08, brief-3)

## Разделы спецификации

История 22, Границы `frontend-auth` (переключатель), швы.

## Demo-guard (обязательно)

- НЕ перезапускать/останавливать `next dev :3000`, `uvicorn :8000`, docker-стенд; НЕ занимать их порты.
- Правки аддитивны: словари ru/en и поведение локали по умолчанию не меняются; демо продолжает открываться как раньше.

## Критерии приёмки

- [ ] Паритет ключей zh = EN 100% в обоих зеркалах словарей (тест считает ключи)
- [ ] Переключение ru/en/zh работает через существующий механизм локали
- [ ] `npm run lint`, `npm test`, `npm run build` — зелёные
- [ ] Демо-стенд не тронут: процессы живы, поведение ru/en прежнее
- [ ] Тест покрывает паритет и fallback недостающего ключа
