# Interfaces — M2 P2 27

| Модуль | Владеет | Выставляет | Прячет |
|---|---|---|---|
| `backend/tokens` | revoke | POST /logout | refresh |
| `frontend/csp` | nonce | CSP | hash |

Швы: pytest ruff mypy npm.
