# 01 — Зелёная база на актуальной main

**Требования:** R03, R06, R10, R02
**Blocked by:** нет
**Зона:** `technozrelost-backend/app`, `technozrelost-backend/tests`, `technozrelost-frontend/src`
**Волна:** 1
**Status:** ready

## Что должно заработать

Весь существующий набор проверок проходит на актуальной базе: бэкенд-тесты зелёные, фронтенд линтит, тесты и сборка проходят. Известная ловушка («npm run build при живом dev-сервере ломает NextAuth-роуты») подтверждена или опровергнута, рабочая процедура зафиксирована. Всё, что упало, починено минимальными безопасными правками или внесено в реестр находок с причиной.

## Из брифа, дословно

> «пару недель назад я доделал фронт + бэк и на локалхосте всё норм работало»
> «куча ошибок»

## Разделы спецификации

Истории 9, 13; Решения (verify); Швы.

## Критерии приёмки

- [ ] БД для тестов поднята штатным compose проекта (без изменения конфигов)
- [ ] Backend pytest — все тесты зелёные; каждое падение либо исправлено с тестом, либо в реестре находок
- [ ] Frontend: `npm run lint`, `npm test`, `npm run build` — зелёные (dev-сервер при сборке остановлен)
- [ ] Список исправлений приложен к отчёту таска (файл:строка, что и зачем)
- [ ] Ни одно изменение не выходит за зону таска; `.env*` не тронуты; история git не переписывалась
