# Отчёт тикета 02 — Гигиена репозитория и карта версий

*Исполнитель: субагент, 2026-08-25. Рабочая копия: `.worktrees/deploy-readiness` (ветка `autopilot/deploy-readiness-code`, 43a98ce). Правки оставлены в рабочем дереве БЕЗ коммита — 294 staged-удаления через `git rm` + правки .gitignore/README + новые документы.*

## Реестр находок

| ID | Область | Файл:строка | Severity | Описание | Действие |
|---|---|---|---|---|---|
| F02-01 | легаси-дубль | `docs/КОД MVP "0" 210726 - ТОЛЬКО ФРОНТЭНД/` (213 файлов) | высоко | Полный снимок фронтенда MVP-0 трекнулся в дереве базы аудита — дублирует актуальный `technozrelost-frontend/`, утяжеляет checkout | **исправлено**: `git rm -r` (восстановимо из истории), без коммита |
| F02-02 | временные файлы | `docs/.scratch/**` (81 файл) | средне | Старые спеки/issues/скриншоты прошлых прогонов (friday-release-candidate, imoscow-product-patterns, mvp1-release) закоммичены вопреки конвенции проекта «`.scratch/` — локальное» (корневой .gitignore уже содержит `.scratch/`) | **исправлено**: `git rm -r docs/.scratch` (восстановимо) |
| F02-03 | битый .gitignore | `docs/.gitignore:1` | высоко | Все базовые паттерны (`node_modules/`, `.env`, `*.log`, `__pycache__/`…) склеены literal `\n` в ОДНУ строку — ни один не работает; дерево было без защиты от рантайм-мусора | **исправлено**: развёрнуто в отдельные строки, все паттерны сохранены |
| F02-04 | отсутствие .gitignore | `.gitignore` (корень базы аудита) | высоко | В базе аудита корневого `.gitignore` нет вовсе: секреты (`.env*`), сборочные артефакты, ОС-мусор не покрыты на уровне репо | **исправлено**: создан (ОС-мусор, `.env*` c negation для `*.example`, Python/Node артефакты, логи, `.backups/`); проверено `git check-ignore` — 9/9 паттернов работают, env-примеры остаются трекаемыми |
| F02-05 | битый .gitignore | корень репо: `.gitignore:1` (ветка main) | высоко | Тот же literal-`\n` дефект первой строки в корневом .gitignore на origin/main — паттерны не работают в основном чекауте | рекомендация: применить то же развёртывание строк (корень репо вне зоны тикета) |
| F02-06 | бинарные дубли в git | `docs/КОНТЕКСТ ПРОЕКТА ТЕХНОЗРЕЛОСТЬ.docx`, `docs/Справочник…(SKILL).docx` (~3 МБ) | низко | Пользовательские docx задвоены: те же файлы лежат в корне репо; бинарники в git растягивают историю. Неотслеживаемое не трогал, из дерева не удалял (функциональные документы владельца) | рекомендация: оставить одну копию вне git (рядом с `ГОСТЫ/`) и вывести из дерева по согласованию |
| F02-07 | неверная вложенность | `docs/docs/agents/domain.md`, `docs/docs/agents/issue-tracker.md` | низко | Агентские доки лежат на уровне глубже, чем ссылается `docs/CLAUDE.md` (`docs/agents/…`); вне зоны тикета — не перемещал | рекомендация: `git mv docs/docs/agents → docs/agents` + сверить ссылки |
| F02-08 | битая ссылка после чистки | `README.md:50-54` | низко | README ссылался на удаляемый `docs/.scratch/friday-release-candidate/release-gate-report.md` | **исправлено**: секция Release gate переписана со ссылкой на историю git |
| F02-09 | ветки | ~34 ветки `codex/*` и др. (50 локальных всего) | средне | 14 веток уже полностью влиты в origin/main; ~15 невлитых с уникальными коммитами (recovery-*, release-integration, internal-ux-redesign…) | рекомендация: план «тег archive/* → bundle → удаление по согласованию» в `docs/version-map.md` §2 |
| F02-10 | worktrees | `.worktrees/` ≈ 9,6 ГБ, 23 чекаута | средне | Мёртвые чекауты слитых веток занимают диск; крупнейшие 1,0–1,8 ГБ каждый | рекомендация: план освобождения до ~0,5 ГБ в `docs/version-map.md` §4 (после merge аудита) |
| F02-11 | расхождение с main | ветка `autopilot/deploy-readiness-code` vs `origin/main` | средне | База аудита впереди на 8 (новости/достижения/консолидация), позади на 2: `be54109` (фикс rewrites фронта) и `a8f85c6` (docs+gitignore) не входят в неё | рекомендация оркестратору: учесть −2 при финальном merge (коммитить/мержить — зона оркестратора) |
| F02-12 | незащищённый мусор | корень репо: `.DS_Store` (untracked) | низко | ОС-мусор в корне; неотслеживаемое правилами не удаляю | рекомендация: покрыт новым `.gitignore` (F02-04); удалить вручную при желании |

## Инвентаризация мусора (сводка)

**Отслеживаемое, выведено из дерева (294 файла, восстановимо):**
- `docs/КОД MVP "0" 210726 - ТОЛЬКО ФРОНТЭНД/` — 213 файлов
- `docs/.scratch/**` — 81 файл

**Отслеживаемое, оставлено (вне зоны/рискованно):** 2 docx под `docs/` (F02-06), `docs/docs/agents/*` (F02-07), корневые `alembic/ db/ app/ tests/ README.md` (зона бэка).

**Неотслеживаемое (НЕ тронуто, только перечислено):** в рабочей копии прогона отсутствует; в корне репо — `.DS_Store`, `.autopilot/`, `.backups/`, `.github/`, `AGENTS.md`, `CODE_REVIEW_2026-08-13.md`, `FULL_CODE_REVIEW_PLAN.md`, `docs/`, `new-front/`, чекауты `technozrelost-*`; игнорируемое — `ГОСТЫ/`, `.scratch/`, `friday-release-candidate/`, `.worktrees/`.

## Верификация

- `git status`: только ожидаемые 294 D (staged), M `README.md`, M `docs/.gitignore`, ?? `.gitignore`, ?? `docs/version-map.md`
- `git check-ignore`: 9/9 новых паттернов срабатывают; `technozrelost-backend/.env.example` и `.env.production.example` остаются трекаемыми (negation работает)
- Зоны вне тикета не тронуты: `git status` пуст по `technozrelost-backend|frontend|app/|tests/|db/|alembic/`
- Коммиты не создавались, история не переписывалась, ветки не удалялись
