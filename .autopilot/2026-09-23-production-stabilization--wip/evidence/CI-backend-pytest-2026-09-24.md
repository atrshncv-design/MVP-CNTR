# CI backend Pytest: расхождение с локальным baseline

Дата: 2026-09-24. GitHub Actions [run 35963159331](https://github.com/atrshncv-design/MVP-CNTR/actions/runs/35963159331) и повторный [run 35963867111](https://github.com/atrshncv-design/MVP-CNTR/actions/runs/35963867111): frontend success, backend Install/dependency audit/Ruff/mypy success, Pytest exit 1. Логи просмотрены в авторизованном браузере владельца; ниже только имена тестов и типы расхождения, без stdout со значениями секретов/данных.

- D09: `tests/test_health.py::test_ready_reports_checked_database_roles` и `test_ready_fails_closed_when_database_check_fails` ожидают `redis=not_configured`, CI получает `redis=ok`, потому что workflow задаёт REDIS_URL и сервис Redis. Локальная точная команда без Redis: 751 passed за 546.74 s; с временным локальным Redis на 6381: первый из этих тестов воспроизведён как failure после 244 passed за 206.90 s.
- D10: `tests/test_infra_contracts.py::test_deploy_generates_256_bit_auth_secrets_without_logging_them` ожидает успешный `deploy.sh check-env`, но CI-процесс наследует `POSTGRES_PASSWORD=change_me` от workflow и guard отвергает его. Это тестовая изоляция env, не повод ослаблять production guard. Реальные секреты не читались.
- D11: шесть `test_tls_deploy_gate.py` positive-case тестов не видят SAN и получают `TLS-GATE: SAN сертификата не содержит PUBLIC_HOST`. `openssl_text(cert, "-ext subjectAltName")` передаёт опцию и аргумент одним argv; в одноразовом Linux-контейнере OpenSSL 3.0.20 combined argv дал пустой stdout с exit 0, раздельные `-ext`, `subjectAltName` вывели SAN синтетического сертификата. Fail-closed политику TLS менять нельзя.
- Всего из лога извлечено 9 FAILED имён (2 readiness, 1 deploy, 6 TLS). Точный pytest summary не сохранён, чтобы не переносить сырые логи. Production, реальные `.env`, ключи и пользовательские данные не затрагивались.
