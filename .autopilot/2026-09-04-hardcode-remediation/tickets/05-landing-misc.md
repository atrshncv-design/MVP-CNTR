# 05 — Лендинг, вход и остаток

**Требования:** R01, R02
**Blocked by:** 04
**Зона:** `technozrelost-frontend/src/app/` (кроме `dashboard/`) + `technozrelost-frontend/src/features/` (кроме `project/`, `registry/`)
**Волна:** 5
**Status:** ready

## Что должно заработать

Лендинг, вход/регистрация, вступление по токену, опросник и все оставшиеся фичи — через перевод; во всём фронте не осталось строк экрана вне словарей.

## Из брифа, дословно

> «вносить правки и исправить хардкод»
> «Особенно критично, когда хардкод касается отображения слов»

## Разделы спецификации

Истории 1–6, Решения §1, Швы.

## Критерии приёмки

- [ ] В зоне не осталось литералов вне `t()` (включая атрибуты и тексты ошибок)
- [ ] Новые ключи — в неймспейсах `landing`/`auth`/`common`, в обе пары словарей ru+en; чужие ключи не тронуты
- [ ] Сканер по всему фронту показывает ноль строк экрана вне словарей, паритет — ноль
- [ ] `npm run lint`, `npm test` — зелёные
