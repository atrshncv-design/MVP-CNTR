# Изначальная задача

> Записано 2026-08-26. Текст задачи не редактируется — он эталон, с которым
> сверяется готовый результат. Всё, что сказано позже, дописывается
> в «Дополнения» ниже.

Да, мне нравятся все твои предложения. Приступай

*(вызвано командой `/autopilot`; относится к предложениям из предыдущего сообщения агента — см. «Дополнения»)*

## Дополнения

- 2026-08-26 — владелец одобрил все пять предложений агента по итогам CTO-интервью:
  1) запустить фазу M0 плана `docs/Plan.md`: закрыть критические дыры безопасности и CI — N-01 (обход модерации вступления), FE-01 (fail-open матрица ролей фронта + регрессионный тест), FE-02 (localhost-fallback прода, единый API-URL), CI-пайплайн (P-12);
  2) подготовить техтребования к серверу заказчика (одностраничная спецификация: ОС, Docker, CPU/RAM/disk, исходящий доступ, их бэкапы) + оценку текущего стека на импортозамещение;
  3) E2E-автотест демо-маршрута начать в октябре (фаза M2 плана) — вне рамок этого прогона;
  4) учения «суббота вечером» раз в месяц после G2 — церемония процесса, вне рамок этого прогона;
  5) тест bus-factor в ноябре — вне рамок этого прогона.
- 2026-08-26 — объём прогона владельцем не ограничивался первой волной: агент расширяет прогон до полного гейта G1 («Безопасность закрыта», до 30.09) = ВСЕ пункты уровня P0 реестра `docs/BACKLOG.md` + CI + техтребования к серверу с импортозамещением. Это соответствует фазе M0 плана (таблица 14 задач).
- 2026-08-26 — критерии приёмки прогона: все P0 в BACKLOG.md → done; CI зелёный на ветке; регрессионный тест матрицы ролей ловит незакрытый маршрут; бэкап создаётся по расписанию; восстановление отрепетировано; техтребования к серверу готовы.
- 2026-08-26 — работа ведётся на ветке `autopilot/m0-security-hardening` от актуального main; merge в main — после приёмки владельцем (правило репозитория).
- 2026-08-26 — брифинг: на вопрос об объёме владелец ответил **«Полный гейт G1»** — все 14 пунктов P0 в этом прогоне, инфраструктура надёжности включается.
