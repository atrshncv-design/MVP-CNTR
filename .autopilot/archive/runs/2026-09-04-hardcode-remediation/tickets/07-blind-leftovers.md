# 07 — Добор пропущенных строк (находки слепой приёмки)

**Требования:** R01, R02
**Blocked by:** 06
**Зона:** точечно (файлы ниже)
**Волна:** 7
**Status:** ready

## Что должно заработать

Остатки экранных строк из вердикта слепой приёмки уведены через перевод; сканер подтверждает отсутствие новых ui-string.

## Из брифа, дословно

> «вносить правки и исправить хардкод»
> «Особенно критично, когда хардкод касается отображения слов»

## Разделы спецификации

Истории 1–6, Решения §1, Швы.

## Критерии приёмки

- [ ] `AiDocConsultant.tsx` — фолбэк «Я консультирую только по документам УГТ» через `t()`
- [ ] `ExportButton.tsx:61` — «Экспорт XLSX не удался» через `t()`
- [ ] `news-admin-api.ts:72` — текст ошибки через `t()` (или код ошибки, если уходит в логику)
- [ ] `offline/queue.ts` — коннекторы «и/через» через параметры перевода
- [ ] `landing-nav` — «Новости» через `t()` (бренд-вордмарк не трогать)
- [ ] `auth.config.ts` — «Пароль»: если лейбл используется — через `t()`, если мёртвый — удалить
- [ ] `wb.creator` — не трогать (бренд-метаданные, зафиксировано)
- [ ] Ключи в обе пары словарей ru/en; `npm run lint`, `npm test` — зелёные; сканер — эти файлы вне ui-string
