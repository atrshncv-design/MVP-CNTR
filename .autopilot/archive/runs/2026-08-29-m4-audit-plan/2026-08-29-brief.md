# Изначальная задача

> Записано 2026-08-29. Текст задачи не редактируется — он эталон, с которым
> сверяется готовый результат. Всё, что сказано позже, дописывается
> в «Дополнения» ниже.

На основе проведённого аудита 2026-08-29 (M3, 14 находок: 2 High, 5 Medium, 4 Low, 3 Info) подготовь полный комплект спецификаций и тикетов для исправления всех найденных ошибок, недоработок, архитектурных проблем и пробелов в тестировании. Преобразуй результаты код-ревью в реалистичный и последовательный план, по которому другой AI-агент сможет реализовать исправления без повторного изучения проекта. Не исправляй код на этом этапе — только документация, спеки и тикеты. Не потеряй ни одного замечания: каждая подтверждённая проблема — в отдельный тикет, либо объединена с логически связанными, либо отложена с обоснованием. План в `docs/remediation-m4/`, спеки в `docs/remediation-m4/specs/`, тикеты в `docs/remediation-m4/tickets/`, зеркало в `.autopilot/2026-08-29-m4-audit-plan/`. Не перезаписывай `docs/remediation/` M3.

## Дополнения

2026-08-29 — владелец подтвердил структуру `docs/remediation-m4/` и предположения Q-01..Q-06 (ок). Принято: Q-01 clamav/clamav официал amd64, Q-02 staff avg + ?all=1 все, Q-03 nginx regex, Q-04 env CVD единый, Q-05 SOPS отложено до B2G, Q-06 technologies ETag per page.
