# 22 — Переключатель языка на входе и в ЛК

**Требования:** G04 (история 27)
**Blocked by:** —
**Зона:** `frontend/shell`
**Волна:** 8
**Status:** ready

## Что должно заработать

Тот же тоггл RU/EN/ZH — на экранах /login и /register и в шапке личных
кабинетов (/dashboard/*); выбранная локаль применяется ко всем строкам этих
экранов (включая тексты ошибок входа).

## Из брифа, дословно

> Схуяли переключение языка недоступно на экранах входа и внутри ЛК? (Дополнения 2026-09-09, brief-6)

## Разделы спецификации

История 27, Границы `frontend-auth` (переключатель), швы.

## Demo-guard (обязательно)

- НЕ перезапускать/останавливать `next dev :3000`, `uvicorn :8000`, docker-стенд; НЕ занимать их порты.
- Правки аддитивны: существующие экраны и поведение ru/en/zh не меняются, добавляется только тоггл.

## Критерии приёмки

- [ ] Тоггл виден и работает на /login, /register и в шапке dashboard (проверено переключением RU/EN/ZH)
- [ ] Строки этих экранов, включая ошибки входа, берутся из словарей активной локали
- [ ] `npm run lint`, `npm test`, `npm run build` — зелёные; `:3000` жив до и после
- [ ] Тест покрывает наличие тоггла и применение локали на экране входа
