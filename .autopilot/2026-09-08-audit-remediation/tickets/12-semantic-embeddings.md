# 12 — Настоящие эмбеддинги и честный rerank

**Требования:** R05i (история 14)
**Blocked by:** 11
**Зона:** `backend/ai`, `db/rag`
**Волна:** 3
**Status:** ready

## Что должно заработать

Семантический поиск — на обучаемой embedding-модели с переиндексацией:
запрос-синоним находит релевантный документ. Rerank в matching меняет
порядок кандидатов, а не только текст объяснений.

## Из брифа, дословно

> составь спеку и тикеты для внесения правок
> «Всё + приоритеты»: спека покрывает все подтверждённые находки; таски — на Blocker/Critical/High/CI (Дополнения 2026-09-08)

## Разделы спецификации

История 14, Решение §8, Границы `ai`.

## Критерии приёмки

- [ ] Запрос-синоним возвращает релевантный документ (набор эталонных пар зелёный)
- [ ] Индекс пересчитан новой моделью; размерность и миграция согласованы
- [ ] Порядок matching меняется при другом ранжировании LLM (не только текст причин)
- [ ] Деградация при недоступности модели — честный fallback, а не молчание
- [ ] Тест на шве HTTP покрывает синонимы и порядок rerank
