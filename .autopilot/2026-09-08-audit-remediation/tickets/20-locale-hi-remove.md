# 20 — Убрать хинди из доступных языков

**Требования:** G03 (история 25)
**Blocked by:** —
**Зона:** `frontend/i18n`
**Волна:** 7
**Status:** ready

## Что должно заработать

Хинди недоступен в интерфейсе: нет кнопки HI в переключателе, нет hi в реестре
локалей и в мэппингах переводчиков, удалены словари hi, их тесты и allowlist;
запрос локали hi ведёт себя как неподдерживаемая локаль (дефолт ru).
Поведение ru/en/zh не меняется.

## Из брифа, дословно

> Там язык хинди до сих пор доступен, хотя я его отменил (Дополнения 2026-09-09, brief-5)

## Разделы спецификации

История 25, Границы `frontend-auth` (переключатель), швы.

## Demo-guard (обязательно)

- НЕ перезапускать/останавливать `next dev :3000`, `uvicorn :8000`, docker-стенд; НЕ занимать их порты.
- Удаляется только hi-слой: ru/en/zh, поведение по умолчанию и демо-данные не меняются.

## Критерии приёмки

- [ ] В переключателе только RU/EN/ZH; hi отсутствует в реестре локалей и мэппингах
- [ ] Файлы `*hi.json`, `locale-hi.test.mjs`, `locale-hi-allowlist.json` удалены; ссылок на hi в коде/тестах нет
- [ ] Запрос hi даёт дефолт ru без падений
- [ ] `npm run lint`, `npm test`, `npm run build` — зелёные; `:3000` жив до и после
- [ ] История git сохраняет отменённый перевод (удаление только в рабочей копии, не переписывание истории)
