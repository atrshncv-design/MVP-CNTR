# 21 — Досев демо-учёток без затирания стенда

**Требования:** BUG-1 (история 26)
**Blocked by:** —
**Зона:** `backend/seeds`
**Волна:** 8
**Status:** ready

## Что должно заработать

Пять утверждённых демо-учёток (gk/rd/manager/admin/investor) существуют в
dev-БД с ролями из сидов; вход каждой — 200 с первого раза. Данные стенда
не затёрты, процессы не перезапускались.

## Из брифа, дословно

> схуяли пароли не работают те, что мы раньше утверждали? (Дополнения 2026-09-09, brief-6)

## Разделы спецификации

История 26, Границы `auth`.

## Диагноз (проверен)

Учёток нет в dev-БД (`demo.*` — 0), пароли не менялись; миграция 0033 трогает
только роли (3 отзыва id 35/36/37), таски 06/14 пути логина не меняют.
Чинить нечего — нужно досеять.

## Demo-guard + секреты (нарушение = провал)

- Только INSERT/UPSERT пяти учёток поверх существующих данных; TRUNCATE/DELETE, миграции, сиды остального, рестарты процессов и контейнеров — запрещены.
- Пароль — именем переменной из сидов (`DEMO_PASSWORD`), значение никуда не выписывать: ни в код, ни в тесты, ни в отчёт, ни в логи. Если переменной нет в окружении — верни BLOCKED с именем, значение не выдумывай.
- Проверочных логинов — минимум (успешные сбрасывают троттлинг, неудачные кормят): по одному на учётку, через HTTP к живому :8000.

## Критерии приёмки

- [ ] В dev-БД ровно досеяны 5 учёток с ролями из сидов; остальные 34 пользователя и данные не тронуты
- [ ] Логин каждой учётки — 200 с первой попытки
- [ ] Роль investor назначена напрямую сидом и переживает логику allowlist/0033 (не через self-register)
- [ ] Процессы :3000/:8000 и контейнеры живы до и после; порты не занимались
- [ ] Тест: идемпотентный reseed-тест на тестовой БД (повторный прогон не плодит дублей)
