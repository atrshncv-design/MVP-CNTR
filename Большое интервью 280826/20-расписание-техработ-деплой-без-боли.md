# 20 — Расписание техработ (безболезненный деплой)

Источник Q21

Разработка локально `compose.yml` `pg-primary:5432` + тесты `pytest`, деплой `deploy.sh` health-gate 300с + rollback `IMAGE_TAG` SHA `compose.prod.yml` — в техокно 02:00-04:00 GMT+4 (нагрузка <1% как ты сказал). CI `ci.yml` гоняет `pytest/ruff/mypy` + `npm` до пуша. Правки anytime — деплой только в окно, анонс в Telegram `alerter.py`.
