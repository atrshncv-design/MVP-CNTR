# 02 — Карта системы на 1 странице (для CTO и будущих агентов)

## Для руководителя (1 абзац)
Платформа Технозрелость — B2B/B2G по ГОСТ Р 58048-2017: проекты НИОКР → УГТ 1-9 → реестры компетенций региона (МТК/ВУЗы/проекты/технологии) → Туно (поиск по реестрам) + Каба-Инмар (помощник по ГОСТ/документам). Стек: Next.js 16 + FastAPI + PostgreSQL 16/pgvector + MinIO/ClamAV + Redis + nginx. Один Docker-хост сейчас — SPOF.

## Поток данных (стрелки)
```
Browser → nginx:443 (TLS) → frontend:3000 (SSR via API_URL_INTERNAL src/lib/public-api.ts:30) 
                         → backend:8000 (FastAPI x2 replicas) 
                         → Primary:5432 (запись) / Replica:5433 (чтение реестров get_read_db() database.py:21)
                         → MinIO:9000 (файлы) → ClamAV:3310 fail-closed file_storage.py:79
                         → RAG pgvector rag_documents vector(1536) → LLM_API_BASE (OpenAI-совм.) ai_assistant.py:31 (no-op без ключа)
                         → Redis (throttle/события, сейчас пустой) + Prom+Grafana (внутри)
```

## Что сломается первым
- При 10К VU: пул 100 коннектов P-01 config.py:38 + SSE N-03 + in-memory throttle P-04
- При уходе CTO: нет runbook, нет deputy, бэкапы на том же диске INF-13

## Цифры пилота vs цели
- Пилот 500 VU: 8 vCPU/32 ГБ, RPO 5м archive_timeout 60s, RTO 1ч rehearse_pitr.sh
- Цель 10К: 8 API×2 vCPU + PG 16/64 + Replica 8/32 + Redis/MinIO — 166 ГБ кластер, pgbouncer обязателен

