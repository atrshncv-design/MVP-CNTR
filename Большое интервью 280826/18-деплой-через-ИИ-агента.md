# 18 — Деплой через ИИ-агента

Источник Q20

Как ты планировал: агент = Maker `AGENTS.md`, ты = Functional Validator. Агент делает `git push` → CI `ci.yml` зелёный → `deploy.sh` `health-gate 300с` + `rollback TAG` git SHA `compose.prod.yml:IMAGE_TAG` → `curl /ready` `health.py:12`.

**Безопасно:** агент не деплоит без `health-gate` и `GRAFANA_PASSWORD` check `deploy.sh:86`, секреты только имена `TELEGRAM_BOT_TOKEN` в `.env`. Ты апрувишь `merge в main` после приёмки `Plan.md:59`.

**Риски:** агент без `--workers` дубля `main.py:56` scheduler, без `pgbouncer` — предупредит. Для 01.10 — агент деплоит на dev `compose.yml` `pg-primary:5432`, прод — после твоей команды.
