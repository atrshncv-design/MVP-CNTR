# DESIGN.md — дизайн-система платформы «Технозрелость»

> Единый источник истины по визуальному языку платформы (фронтенд `technozrelost-frontend`).
> Статус: **v1 — 03.08** (гибрид: DESIGN.md → роллаут токенов → экраны демо-маршрута №18).
> Система в одном предложении: **государственная точность + инженерная ясность** — тёмно-синий и синий акцент бренда, серифная типографика заголовков (PT Serif), моноширинные идентификаторы и уровни УГТ (JetBrains Mono), спокойные светлые поверхности, один акцент на экран.

---

## 1. Принципы (posture)

1. **Институция, а не стартап.** Платформа центра НТР: строго, предсказуемо, без декоративных излишеств. Любая деталь должна объяснять процесс (статус, уровень, очередь).
2. **УГТ — главный герой.** Уровень технологической готовности — центральная метрика платформы: он в шапке карточки, в реестрах, в очередях менеджера. Уровни — всегда моноширинные бейджи `УГТ N`, уровни 7+ — тёмные (витрина технологий).
3. **Один акцент.** Синий `--tz-accent` используется не более двух раз на экран (обычно: первичная CTA + ключевая метрика/ссылка). Всё остальное — нейтраль: поверхности, текст, бордеры.
4. **Честные состояния.** Нет заглушек и выдуманных данных: пустое состояние объясняет, *что появится* и *когда*. Новые механики (очереди, автозаявка) до появления бэкенда показываются как честные empty-state, а не моки.
5. **Иерархия через типографику, а не цвет.** Заголовок (сериф) → подпись (sans) → метаданные (mono, капс). Цвет подсвечивает только статус и акцент.
6. **Эмодзи запрещены** (рендерятся ч/б в Chrome на macOS): только SVG-иконки `lucide-react`.

---

## 2. Палитра (OKLch-токены)

База — существующая гамма бренда (`#0f172a` navy, `#2e5bff` blue, `#f5f7fa` фон). Все значения выражены в OKLch; hex в комментариях — только для справки, в коде использовать токены.

### Нейтраль (поверхности и текст)

| Токен | OKLch | Hex (справка) | Назначение |
|---|---|---|---|
| `--color-tz-bg` | `oklch(97.55% 0.005 258.3)` | `#f5f7fa` | Фон страницы |
| `--color-tz-surface` | `oklch(100% 0 0)` | `#ffffff` | Карточки, панели |
| `--color-tz-fg` | `oklch(20.77% 0.040 265.8)` | `#0f172a` | Основной текст |
| `--color-tz-secondary` | `oklch(44.55% 0.037 257.3)` | `#475569` | Вторичный текст |
| `--color-tz-muted` | `oklch(55.44% 0.041 257.4)` | `#64748b` | Метаданные, подписи |
| `--color-tz-border` | `oklch(91.94% 0.012 252.1)` | `#dfe5ec` | Границы секций |
| `--color-tz-card-border` | `oklch(94.12% 0.007 247.9)` | `#e8ecf0` | Границы карточек |
| `--color-tz-soft` | `oklch(93.5% 0.008 255)` | — | Заполнение подложек (hover) |

### Бренд (navy + акцент)

| Токен | OKLch | Hex (справка) | Назначение |
|---|---|---|---|
| `--color-tz-accent` | `oklch(55.16% 0.247 266.0)` | `#2e5bff` | Первичное действие, ссылки, активные состояния |
| `--color-tz-accent-hover` | `oklch(63.47% 0.195 263.6)` | `#4a82ff` | Hover акцента |
| `--color-tz-accent-soft` | `oklch(95.50% 0.021 268.4)` | `#eaf0ff` | Подложка акцента (бейджи, иконки) |
| `--color-tz-hero` | `oklch(20.77% 0.040 265.8)` | `#0f172a` | Тёмные поверхности: топбар, hero, бейджи УГТ 7+ |
| `--color-tz-hero-text` | `oklch(98.42% 0.003 247.9)` | `#f8fafc` | Текст на тёмном |
| `--color-tz-hero-muted` | `oklch(71.07% 0.035 256.8)` | `#94a3b8` | Метаданные на тёмном |
| `--color-tz-hero-border` | `oklch(37.17% 0.039 257.3)` | `#334155` | Границы на тёмном |

### Статусы (семантика)

| Токен | OKLch | Hex | Статусы |
|---|---|---|---|
| `--color-tz-success` | `oklch(69.59% 0.149 162.5)` | `#10b981` | Активен, подтверждён, завершён, УГТ повышен |
| `--color-tz-warning` | `oklch(76.86% 0.165 70.1)` | `#f59e0b` | Внимание, требует документов |
| `--color-tz-review` | `oklch(83.43% 0.151 96.7)` | `#e5c840` | На проверке / на рассмотрении |
| `--color-tz-danger` | `oklch(63.68% 0.208 25.3)` | `#ef4444` | Отклонён, ошибка |
| `--color-tz-neutral` | `oklch(71.07% 0.035 256.8)` | `#94a3b8` | Черновик, архив |

Для каждого статусного цвета есть **мягкая подложка** — `color-mix(in oklch, <цвет> 12%, white)` — для бейджей и подложек иконок. В CSS заданы готовые пары: `--color-tz-success-soft`, `--color-tz-warning-soft`, `--color-tz-review-soft`, `--color-tz-danger-soft`.

---

## 3. Типографика

Три семьи, загружаются через `next/font/google` (self-hosted):

| Роль | Шрифт | CSS-переменная | Применение |
|---|---|---|---|
| Display | **PT Serif** (400/700) | `--font-pt_serif` | Только заголовки страниц/секций (`h1`, пустые состояния), крупные цифры-метрики не входят |
| Body | **Inter** (400/500/600/700) | `--font-inter` | Текст, кнопки, формы, навигация |
| Mono | **JetBrains Mono** (400/500/700) | `--font-jetbrains_mono` | ID (`ЦНТР-12`), уровни (`УГТ 4`), токены (`TZ-XXXXXX`), eyebrow-подписи, числовые метрики |

Правила:
- **Сериф — только заголовки.** `h1` страниц ЛК (30–36px), заголовки пустых состояний (24px), крупные заголовки карточек не обязательны. Никогда — для текста, кнопок, таблиц.
- **Цифры и ID — моноширинно** (табличные цифры, стабильная ширина в списках).
- `font-feature-settings: "tnum"` для числовых колонок.
- Размерная шкала (clamp):

| Ступень | Размер / строка | Класс |
|---|---|---|
| Page title | `clamp(1.75rem, 2.2vw + 1rem, 2.25rem)` / 1.15 | `tz-page-title` |
| Section title | `1.125rem` / 1.3, 700 | `tz-section-title` |
| Card title | `1rem` / 1.35, 700 | — |
| Body | `0.9375rem` / 1.55 | — |
| Small | `0.8125rem` / 1.45 | — |
| Eyebrow | `0.6875rem`, mono, капс, `letter-spacing 0.08em` | `tz-eyebrow` |
| Caption | `0.75rem`, muted | — |

---

## 4. Геометрия и глубина

- **Радиусы:** карточки `16px` (`--radius-tz-card`), кнопки/инпуты `10px` (`--radius-tz-btn`), бейджи/чипы `999px`, иконки-тайлы `12px`.
- **Тени:** карточка `0 1px 2px rgb(15 23 42 / 0.04)` (`--shadow-tz-card`); hover карточки — `0 8px 24px rgb(15 23 42 / 0.08)` + `border-color` акцента (`--shadow-tz-pop`); тёмные поверхности — без теней, на контрасте фона.
- **Отступы:** сетка 4px; стандартные паддинги карточек `20px`; секции — `24–32px`; сетка контента — `max-w-[1280px]`, колонки 12.
- **Переходы:** 150–200ms `cubic-bezier(0.4, 0, 0.2, 1)` на hover/focus; появление контента — framer-motion (opacity + `y: 8–14`, каскад `0.08 × idx`), как в текущих ЛК.

---

## 5. Компоненты

Классы живут в `globals.css` (слой `@layer components`), префикс `tz-`.

### Кнопки
- `.tz-btn` — база (inline-flex, радиус 10, 600, padding `10px 16px`, transition).
- `.tz-btn-primary` — заливка `--color-tz-accent`, текст белый; hover `--color-tz-accent-hover`; актив — сжатие.
- `.tz-btn-secondary` — белая, бордер `--color-tz-border`, текст `--color-tz-fg`; hover — бордер `--color-tz-accent`/тень.
- `.tz-btn-ghost` — без фона, текст `--color-tz-muted`; hover — `--color-tz-soft`.
- `.tz-btn-danger` — заливка `--color-tz-danger`.
- `.tz-btn-sm` — `8px 12px`, 13px.
- Focus-visible — кольцо `2px --color-tz-accent` c `offset 2px`.

### Карточки
- `.tz-card` — `bg-surface`, `border card-border`, радиус 16, `--shadow-tz-card`; hover (для кликабельных) — бордер акцента + `--shadow-tz-pop`.
- `.tz-card-hover` — интерактивная карточка (Link).

### Бейджи и уровни УГТ
- `.tz-badge` — пилюля, 11px, 500, `background: color-mix(12%)`, текст — сам цвет.
- Модификаторы: `.tz-badge-success | -warning | -review | -danger | -neutral | -accent`.
- `.tz-ugt` — моноширинный бейдж уровня: подложка `--color-tz-accent-soft`, текст акцент; `.tz-ugt-strong` (УГТ 7+) — заливка `--color-tz-hero`, текст `--color-tz-hero-text`.

### Формы
- `.tz-input`, `.tz-select`, `.tz-textarea` — белые, бордер `--color-tz-border`, радиус 10, `14px`; focus — бордер акцента + мягкое кольцо; placeholder — `--color-tz-muted`.
- `.tz-label` — 13px, 500, `--color-tz-secondary`.
- `.tz-field` — обёртка: label + control + помощь.

### Навигация ЛК
- Топбар: тёмный (`--color-tz-hero`), высота 72px, бренд mono капс `ТЕХНОЗРЕЛОСТЬ` + акцентная точка; пункты навигации — `--color-tz-hero-muted`, hover/active — белый + подложка `--color-tz-hero-border`; справа — имя пользователя (sans 600) + роль (muted) + «Выйти».
- Внутристраничные табы: `.tz-tabs` (бордер снизу `--color-tz-border`), `.tz-tab` (падинг 16px), `.tz-tab-active` — `border-bottom 2px --color-tz-accent`, текст `--color-tz-fg`, 600.

### Прочее
- `.tz-eyebrow` — mono-подпись над заголовком.
- `.tz-stat` — стат-карта: label (13px muted) + значение (mono, 28–30px, 700) + иконка-тайл на подложке цвета.
- `.tz-empty` — пустое состояние: иконка-тайл, заголовок (сериф 24px), пояснение (muted), опциональная CTA.
- `.tz-progress` — полоса прогресса УГТ (4–6px, подложка `--color-tz-soft`, заливка акцент).
- `.tz-chip` — фильтр-пилюля: `--color-tz-soft` подложка, hover темнее; активный — заливка акцент, текст белый.
- `.tz-kbd`/`.tz-token` — моноширинный инлайн-бейдж токена (join-токен `TZ-XXXXXX`): подложка `--color-tz-soft`, mono 700.

---

## 6. Паттерны экранов демо-маршрута №18

1. **ЛК любой роли**: eyebrow (роль) → серифный `h1` «Добро пожаловать, …» → подпись → карточка **«Оценить УГТ»** (первичная CTA + счётчик черновиков) → 4 стат-карты → контент.
2. **Карточка проекта**: шапка = статус-бейдж + `ЦНТР-{id}` (mono) + серифный заголовок; справа — УГТ-бейджи `текущий → целевой`; затем радар (recharts) и блок «Этап N→N+1» (требования комплекта, статус, лента заявки — после тикета 23).
3. **ЛК менеджера**: табы «Новые проекты» / «Заявки на повышение УГТ» (счётчики); карточка очереди: имя, УГТ-бейджи, предварительная оценка, кнопки «Подтвердить и присвоить УГТ N» / «Отклонить с причиной».
4. **Реестры**: переключатель «Проекты» / «Технологии УГТ 7+»; фильтры-чипы (статус, категория, диапазон УГТ) + поиск; карточка: mono-ID, статус, УГТ-бейдж, бюджет/исполнитель, прогресс.
5. **Пустое состояние очереди**: иконка-тайл, серифный заголовок, пояснение «Заявки появятся автоматически при полноте комплекта документов этапа».

---

## 7. Правила вёрстки (check)

- [ ] Один акцент ≤ 2× на экран; статусные цвета — только для статусов.
- [ ] Сериф — только заголовки; цифры/ID/УГТ — mono; никаких эмодзи — только lucide.
- [ ] Новые значения цветов — только через токены (`var(--color-tz-*)` или утилиты `bg-tz-*`), не через инлайн-hex.
- [ ] Пустые состояния объясняют, что появится; без моков и выдуманных цифр.
- [ ] Hover/focus-состояния на всех интерактивных элементах; focus-visible кольцо.
- [ ] Мобильная версия — редизайн, не сжатие: сетки схлопываются (`grid-cols-1`), без горизонтального скролла.
- [ ] `data-od-id` на секциях экранов демо-маршрута.

---

## 8. Легаси-токены

Старые `--tz-*` переменные (тикет 16) сохранены в `globals.css` как алиасы новых значений — существующие страницы не ломаются. Постепенно страницы переводятся на классы и утилиты системы.
