# 04 — Реестры и Туно vs Каба-Инмар

## Хаос ingest
API/ручной/пользователи → staging_nioktr_raw (JSONB) → валидатор (ogrn Hash P-05) → moderation_queue (CTO) → public.nioktr_cards + tuno RAG только public. Пока 65 пунктов BACKLOG, но будет расти.

## Туно vs Каба
Сейчас один контур rag_documents vector(1536) одна таблица 0002_rag_documents.sql, один индекс ivfflat, один LLM_MODEL config.py:57.
Нужно: migration 0028 contour TEXT CHECK(tuno,kaba), два частичных индекса WHERE contour=..., POST /chat/tuno (реестры) vs /chat/kaba (ГОСТ), ingest с contour.

## PII правило
Строжайшее: ПДн не в облако, в MVP только скрипты. Идеал локальная→облачная, но в декабре gateway_enabled=false + allowlist.


## Закрытые — вне платформы (Q22)
Платформа хранит только открытые разрешённые данные. Закрытые — лично к CTO вне ЛК, без функционала «запросить закрытое» — меньше путаницы и 152-ФЗ рисков.
