# 30 — Фронтенд: роль «Регулирующая организация» + переименование

**What to build:** Переименование «Эксперт УГТ» → «Регулирующая организация» во всех UI-местах (список ролей при регистрации, ЛК, join-форма, селекторы ролей) и новый ЛК: присоединение к проекту по токену TZ-XXXXXX (форма join уже есть — переиспользуем), список присоединённых проектов, добавление верифицирующих документов (подтверждение УГТ) к карточке, статус «материал передан менеджеру». Существующая страница `/dashboard/ugt_expert` переезжает на новый slug/название (роуты и навигация обновляются).

**Blocked by:** 25 — API верифицирующих документов; 20 — схема (переименование роли в БД)

**Status:** ready-for-agent

- [ ] Роль в списке при регистрации: «Регулирующая организация» (slug `regulating_organization`), без «Эксперт УГТ» в UI
- [ ] ЛК: join по токену → список присоединённых проектов с УГТ
- [ ] В карточке присоединённого проекта — «Добавить верифицирующий документ» (подтверждение УГТ) + статус «передано менеджеру»
- [ ] Навигация/роуты: `/dashboard/ugt_expert` → новый путь, все ссылки и guards обновлены (`roles.ts`)
- [ ] Смоук: регистрация как регулирующая организация → join по токену → загрузка документа → видно менеджеру
