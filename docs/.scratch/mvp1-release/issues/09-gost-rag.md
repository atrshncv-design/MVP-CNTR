# 09 — ГОСТы в RAG-базу знаний

**What to build:** Скрипт-загрузчик нормативных документов: парсинг PDF (текстовый слой; OCR для сканов), чанкинг по разделам, запись в `rag_documents` (`doc_type=gost`, `source_uri` = файл + раздел), расчёт эмбеддингов, идемпотентность по хэшу. Источник — папка «ГОСТЫ» (соберёт пользователь; без неё работает с `GOST-R-58048-2017.pdf` в корне проекта).

**Blocked by:** 02 — Тест-инфраструктура бэкенда

**Status:** ready-for-agent

- [ ] ГОСТы загружены в RAG-базу, поиск возвращает релевантные чанки с указанием раздела
- [ ] Повторный запуск не дублирует документы
- [ ] Ассистент отвечает по ГОСТу с источниками
