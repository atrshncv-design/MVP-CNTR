# Ремедиация по аудиту 2026-08-29 — план исправлений

Источник: аудит M2 P2 харденинга (27 пунктов) + проверка P0/P1 внешних критериев. Аудит: 347 backend / 39 frontend тестов PASS, но 3 High / 7 Medium / 6 Low / 4 Info недоработок + dirty `uv.lock`.

Папка `docs/remediation/` — полный комплект для другого AI-агента, без необходимости переизучать код.

- `PLAN.md` — сводная карта проблема→спека→тикет, этапы, граф зависимостей, чек-лист полноты
- `specs/SPEC-01..08` — проверяемые спецификации (что строить и как проверить)
- `tickets/TICKET-01..17` — атомарные тикеты с критериями приёмки и командами

Форматы взяты из `docs/BACKLOG.md` и `AGENTS.md` (Serial/BigSerial, Hash/B-Tree, `public/test` схемы). Не перезаписывает `docs/BACKLOG.md`, `docs/adr/*`.

Порядок: Этап 0 вопросы → Этап 1 P0 (TICKET-01..05,10) → Этап 2 P1 (06..09,11) → Этап 3 P2 (12..15) → Этап 4 тесты/операционка (16..17).

Запуск агента: `Продолжи автопилот` не требуется — это вне `.autopilot/`; исполняй тикеты по `PLAN.md: Этапы` волнами, каждый тикет — отдельный subagent (`phases/5-subagents.md` правило: оркестратор не пишет код).
