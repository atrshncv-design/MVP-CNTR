# Code Review кандидатной сборки — 13.08.2026

**Дата ревью:** 2026-08-13
**Кандидатная сборка:**
- Backend: `technozrelost-backend/` — ветка `release/friday-rc` @ `9e6cccc` (снапшот 06.08, 22/22 тикета; миграции до 0026)
- Frontend: `.worktrees/internal-ux-redesign/` — ветка `codex/internal-ux-redesign` @ `5f1bc7d` («establish frontend baseline 120826», внутренний UX-контур)
- Документация: корень репо `codex/130826` @ `c2964a2`
- **Исключены:** `new-front` (отдельная линия-эксперимент), прод-БД `technozrelost` (read-only evidence)
**Метод:** 8 проходов — гейты (pytest/ruff/mypy/npm test/lint/tsc/build), статические аудиты бэка/фронта, зависимости+инфра, live API-прогон демо-маршрута на тест-БД, браузерный QA (headless Chrome), ре-верификация дефектов ревью 04.08, сборка. Код не изменялся; коммиты не выполнялись.
**Режим:** независимая приёмка к сдаче 31.08.2026 (пользователь — Functional Validator; автопилот, полуавтомат).
**Evidence:** `.scratch/code-review-130826/findings/01…07` + скриншоты `screenshots/`.

---

## Executive verdict

**Release readiness: CONDITIONAL** (демо-путь работает end-to-end; до 31.08 обязательны волны ремонта P0/P1)

- **P0: 1** (RAG-001 — перенесён из 04.08, подтверждён live)
- **P1: 7** (BE-101, DEP-002, INFRA-004, INFRA-005, REV-01, REV-02, REV-03)
- **P2: 6** (BE-102, BE-103, DEP-003, FE-101, INFRA-002, REV-04)
- **P3: 17** (BE-104…106, FE-102…111, FE-005, FE-007, FE-UX-003, BE-LOGIC-003, INFRA-006/007, DEP-004, REV-05/06)
- Новых находок: **28** · Ре-верификация 04.08: **15 FIXED / 7 STILL PRESENT / 2 N/A** (из 24) · Not-a-bug: 11 проверено, 9 подтверждены без изменений

**Главное:** ядро продукта — экспресс-оценка → менеджер → реестр → этапы N→N+1 → join → генерация → чат — **работает live** (регистрация 9 ролей, staff-блок 403, оценка с preliminary, очереди менеджера approve/reject, реестры с ugt_min=7, join pending→active, verification-docs 403→201, генерация ТЗ/Паспорта с персистом контента, реальный LLM в предоценке этапа). Из 19 дефектов ревью 04.08 закрыты 15, включая P1-блокеры INFRA-001 и FE-001, и P0-кандидат RAG-002.

**Три класса проблем до релиза:**
1. **Продуктовые P0/P1:** RAG по-прежнему не извлекает (hash-векторизатор, similarity 0.12 при точных терминах — ассистент отвечает из памяти LLM, не из ГОСТов); авто-подтверждённые проекты УГТ 1–2 навсегда заблокированы для доработки N→N+1 (BE-101); переоценка возможна для 0-уровневых проектов, которые ещё и публикуются в реестр (BE-102).
2. **Процессные P1 (release-blockers):** прод-БД на чужой ревизии 0038 против канона 0026 (апгрейд упадёт); ветка `release/friday-rc` НЕ содержит код в HEAD (весь код untracked) + CI backend-job структурно сломается; прод/CI собирают фронт из `c4f0794`, а не из кандидата `5f1bc7d`; npm-тесты кандидата 16/20 (2 новых фейла).
3. **Мелкие P2/P3** — таймауты fetch, контраст, a11y-остатки, security-заголовки nginx, эмодзи в title и т.п.

---

## Evidence и ограничения

**Реально выполнено (выборка):**
- `pytest -q` → **278 passed** (6:29), `ruff` clean, `mypy` strict — **0 ошибок** (53 файла); `alembic heads` = 0026, тест-БД на head
- Frontend кандидата: `npm test` **16/20** (4 fail: 2 baseline + 2 новых), lint 0 errors/8 warnings, tsc clean, `npm run build` **exit 0, 37 маршрутов**
- `npm audit` → 2 high (nanoid prod, js-yaml dev); `uv audit` → ecdsa 0.19.2 (2 CVE, no-fix); `security_check.py --skip-live` → ALL CHECKS PASS
- `docker compose -f infra/docker-compose.prod.yml config --quiet` → exit 0 (с env); без env → exit 1 (fail-fast работает)
- Live API на тест-БД (:8001→:8000): полный демо-маршрут, включая негативные проверки (403/404/409) и ре-верификацию 10 дефектов 04.08
- Браузер: 11 блоков QA, 11 скриншотов (`.scratch/code-review-130826/screenshots/`)

**Ограничения / блокеры:**
- LLM-провайдер в этом окружении отвечает 55–60 с → `/chat` в фолбэке, предоценка этапа → `evaluation_unavailable`, approve промоушена N→N+1 live не выполнен (**ограничение окружения, не дефект кода**; код честно не делает auto-pass)
- Полный `docker compose build` не запускался (только config + чтение Dockerfile)
- CI в GitHub Actions локально не гонялся (оценён по валидности + структуре веток — вывод INFRA-004 доказан `ls-tree`)
- `.env` значения не читались в отчёт; прод-БД — только `alembic current` (read-only)
- Визуальный контраст оценён по DOM/скриншотам, не по WCAG-инструменту

---

## Факт-таблица приёмки (ключевые требования спеки)

| Требование | Frontend | Backend | Live-доказательство | Статус |
|---|---|---|---|---|
| Регистрация 9 ролей, без seed-логинов | register page | `/auth/register` | live: 201, роли выдаются | **PASS** |
| Staff-роли не регистрируются публично | роль скрыта в UI | 403 `CNTR_STAFF_SLUGS` | live: register cntr_manager → 403 | **PASS** |
| Экспресс-оценка → черновик с preliminary | визард 22 рубежа | `/assessments` | live: 201, preliminary=9, current=2 (cap); браузер: визард→сохранение→карточка | **PASS** |
| Переоценка запрещена (403) | — | guard до 422 | live: повтор (пустой и полный payload) → 403 | **PASS** (кроме дыры BE-102 при preliminary=0) |
| Очередь менеджера, approve/reject с причиной | ЛК менеджера | `/manager/queue/*` | live: approve → published lvl2; reject → rejected; браузер: очереди+кнопки | **PASS** (но `window.prompt` — FE-UX-003) |
| Реестр + тех-реестр `ugt_min=7` | technologies/investor | `/projects/registry` | live: published виден, ugt_min=7 пуст до 7 | **PASS** |
| Этапы N→N+1, автозаявка по комплекту | stage-панель | `/stage-*` | live: словарь этапа 200, загрузка→автозаявка, апдейт без дубля (BE-LOGIC-001 закрыт) | **PARTIAL** (BE-101: auto_confirmed заблокирован; LLM-латентность не дала approve live) |
| Предварительная LLM-оценка по содержанию | — | `_evaluate` | live: реальный русский LLM-ответ | **PARTIAL** (BE-103: содержание PDF не доходит — оценка по заголовку) |
| Join-токен TZ-XXXXXX, статусы | join-форма + `/join/[token]` | `/projects/join` | live: pending→active; браузер: `/join/TZ-…` рендерится (FE-001 закрыт) | **PASS** |
| Verification-docs только после join | verification-панель | 403 до join | live: 403→201 | **PASS** |
| Генерация ТЗ/Паспорта/ТЭО с сохранением | кнопки в карточке | generate + `file_url` | live: tz/passport 200, контент персистится (BE-LOGIC-004 закрыт) | **PASS** |
| AI-ассистент с цитированием ГОСТов | ai-assistant | `/chat`+RAG | live: LLM-фолбэк (латентность); RAG similarity 0.12 | **FAIL** (RAG-001, P0) |
| 9 ЛК на реальных данных, без моков | все ЛК | — | браузер: ЛК ГК + менеджер с реальными счётчиками | **PASS** (лендинг-витрина — мок, FE-103) |
| Тема/адаптивность | light-only (решение) | — | браузер: 375px без overflow, бургер; контраст-остатки FE-104 | **PASS** (с оговорками) |
| Прод-стек одной командой | — | compose/Dockerfile/nginx | config exit 0; build не гонялся; CI сломается (INFRA-004) | **PARTIAL** |
| Гейты зелёные | 16/20 npm test | 278 pytest, mypy 0 | прогоны 13.08 | **PARTIAL** (REV-05) |

---

## Подтверждённые находки (новые, 13.08)

### P0

**RAG-001 [P0] — подтверждён live (перенесён из 04.08).** Hash-векторизатор `sha256(token) % 1536` (`app/core/embeddings.py:16-30`) даёт similarity **0.1204** при точных терминах «уровни готовности технологий» (`/rag/search`, findings 05 §6.2). Ассистент отвечает из памяти LLM, не из ГОСТов; источник — фолбэк спеки §5 так и не заменён реальным эмбеддинг-провайдером.

### P1

**BE-101 [P1] — авто-подтверждённые проекты (УГТ 1–2) навсегда заблокированы для этапов N→N+1.** Единственное присвоение `status="published"` — `manager.py:131` (только для `draft`); `auto_confirmed` создаётся в `assessments.py:194-199`, `publish_project` меняет только `is_public`. Live: publish УГТ-0 проекта → 200, статус остался `auto_confirmed`; `stage-requirements` → 409 «Проект ещё не опубликован менеджером». Ядровая механика доработки документами недоступна целому классу проектов.

**DEP-002 [P1] — частично закрыт: 2 high остались.** sharp-уязвимости устранены; `lucide-react@1.25.0` — официальный пакет npmjs (подозрение снято, integrity подтверждён); остались **nanoid 3.3.16 (HIGH, prod-дерево через next/postcss)** и **js-yaml (HIGH, dev-only eslint)** — фиксятся `npm audit fix`.

**INFRA-004 [P1] — CI backend-job сломается.** Ветка `release/friday-rc` имеет код во вложенном `technozrelost-backend/` (186 файлов, pyproject уровнем глубже), а job делает checkout `path: technozrelost-backend` + `working-directory: technozrelost-backend` → `uv sync` не найдёт манифест (доказано `ls-tree`). Нужно исправить структуру ветки или job.

**INFRA-005 [P1] — прод-БД на чужой ревизии 0038.** `alembic current` (read-only) → `0038`; кандидат head `0026`. `alembic upgrade head` перед релизом упадёт. Открытый вопрос владельцу: процедура выравнивания.

**REV-01 [P1] — прод/CI собирают фронт не из кандидата.** Prod-compose (context `../../technozrelost-frontend`) и CI собирают `c4f0794` (baseline), а ревью-кандидат — `5f1bc7d` (internal-ux-redesign). Все FE-FIXED доказаны на `5f1bc7d` — в прод-артефакт не попадут, пока источник не синхронизирован.

**REV-02 [P1] — backend-ветка не содержит код в HEAD.** 612 изменений в worktree: весь код (`app/`, `tests/`, `alembic/`, `pyproject.toml`) — untracked; коммит `9e6cccc` содержит только README/docs/вложенные каталоги. Пуш-контракт AGENTS.md нарушен; ветка не отражает сборку (в сочетании с INFRA-004 — CI и релиз из ветки невозможны).

**REV-03 [P1] — прод-БД 0038 vs 0026** (см. INFRA-005 — эквивалент, дубль для наглядности матрицы релиза).

### P2

**BE-102 [P2] — переоценка возможна при preliminary=0; 0-уровневые auto_confirmed публикуются в реестр.** Guard (`assessments.py:152-158`: `current_level > 0 OR preliminary IS NULL`) не срабатывает на 0-проекте. Live: слабые ответы → 201 (proj4, lvl0); повтор → 201 новый проект id=5; proj4 виден в `registry`.

**BE-103 [P2] — LLM-предоценка идёт по названиям, не по содержанию; карантинные файлы участвуют в оценке.** `stages.py:132` передаёт LLM `title` + первые 600 символов `file_url`, который для storage-загрузок всегда None (`upload_stage_document_file` не заполняет `file_url`); `_evaluate` не фильтрует `scan_status` (в отличие от подсчёта комплекта). Live: LLM честно ответил «Загружен только заголовок документа… без самого содержания протокола», success=false.

**DEP-003 [P2] — ecdsa 0.19.2 (2 CVE, no-fix, транзитив через python-jose).** Латентно (прод использует HS256), `security_check.py` → PASS c WARN. Мониторить/заменить python-jose.

**FE-101 [P2] — клиентские fetch без таймаута (~30 файлов).** Единственный таймаут — в неиспользуемом `api-client.ts:182` (1 потребитель). При медленном бэке страницы висят в loading бесконечно.

**INFRA-002 [P2] — dev/prod скев Python:** dev 3.14 / Dockerfile 3.12 / CI 3.11 (не закрыт из 04.08).

**REV-04 [P2] — BLOCKED-2 закрыт по факту** (см. ниже: браузерный QA выполнен в этом цикле, findings/06 есть). Пункт помечен для полноты: ре-верификация 04.08 была построена до завершения тикета 06.

### P3 (кратко)

- **BE-104** — гонка автотриггера: IntegrityError → 500 (остаток BE-LOGIC-005; backstop-индекс 0013 есть, приложение не ловит) · **BE-105** — индекс refresh_tokens btree вместо hash (конвенция AGENTS.md) · **BE-106** — мёртвая ветка `status="approved"` в `publish_project` · **BE-LOGIC-003** — старый join-токен в audit_trail полной строкой (не закрыт) · **FE-005** — gk_customer молча глотает ошибки (не закрыт) · **FE-007** — мёртвый api-client, raw fetch в 32 файлах, API_URL в 31 (не закрыт) · **FE-UX-003** — `window.prompt` ×3 у менеджера, подтверждён live (не закрыт) · **FE-102** — fallback `127.0.0.1:8000` в 31 файле + неиспользуемые rewrites · **FE-103** — витрина лендинга `/projects` на мок-данных `showcase.ts` · **FE-104** — остаточная палитра вне токенов (контраст ~1.3–2:1) · **FE-105** — a11y: лендинг без skip-link, nav без aria-label, tablist менеджера без role=tab · **FE-106** — `ROLE_DASHBOARD[primary]` без fallback → redirect(undefined) для неизвестных ролей · **FE-107** — TECH_CATEGORIES в визарде хардкодом (рассинхрон с реестром) · **FE-108** — дубли токенов (warning==review, slate=gray) · **FE-109** — пункт «Документы» ведёт в AI-ассистента · **FE-110** — project/[id] молча роняет страницу при ошибке · **FE-111** — эмодзи 🐴 в title карточки проекта · **INFRA-006** — nginx без security-заголовков · **INFRA-007** — плавающие теги образов; certs только через deploy.sh · **DEP-004** — frontend Dockerfile без USER/HEALTHCHECK · **REV-05** — npm test кандидата 16/20 (2 новых фейла: ugt-trajectory stale LEVELS, ui-shell «радар в aside») · **REV-06** — расхождение тикет-факт по фронт-ветке (документировано)

---

## Ре-верификация дефектов ревью 04.08 (сводка; полная матрица — findings/07)

| Статус | Пункты |
|---|---|
| **FIXED (15)** | RAG-002 (миграция 0012 + live), INFRA-001 (context исправлен, config exit 0), FE-001 (/join/[token] — live), FE-002 (категории из данных — live), FE-003 (light-only по дизайн-решению), FE-004 (N+1 убран), FE-006 (soft0 ушёл), FE-008 (questionnaire-data удалён), FE-009 (код + live), BE-LOGIC-001 (live: апдейт без дубля), BE-LOGIC-002 (честный evaluation_unavailable), BE-LOGIC-004 (live: контент персистится), BE-LOGIC-005 (backstop-индекс 0013), BE-VALID-002 (live: 403 оба payload), BLOCKED-1 (mypy strict — 0 ошибок) |
| **STILL PRESENT (7)** | RAG-001 (live 0.12), INFRA-002 (3.14/3.12/3.11), DEP-002 (2 high), FE-005 (silent zeros), FE-007 (api-client мёртв), FE-UX-003 (window.prompt — live), BE-LOGIC-003 (токен в аудите — live) |
| **N/A (2)** | INFRA-003 (словарь этапов — процесс/данные, вопрос владельцу), BLOCKED-2 (был N/A на момент 07; закрыт фактом тикета 06) |

**Not-a-bug 04.08 (11):** 9 подтверждены без изменений (в т.ч. IDOR-404, staff-403, скор консервативен, ugt_min=7 фильтр, предоценка — реальный LLM). №7 — с оговоркой (новая грань BE-103: оценка по заголовку). №10 — изменился: реальный гэп BE-LOGIC-004 закрыт.

---

## Открытые вопросы (владельцу/Functional Validator)

1. **Прод-БД 0038 vs канон 0026** — согласовать процедуру выравнивания (откат/бэкап/сквош) до релиза. БД не трогали.
2. **Какой фронт идёт в прод: `5f1bc7d` (кандидат) или `c4f0794` (baseline)?** Синхронизировать compose/CI/ветку. Сейчас прод собирает `c4f0794`, все FE-фиксы — в `5f1bc7d`.
3. **Backend-код не закоммичен в `release/friday-rc`** (untracked) + структура ветки ломает CI — план коммита/перебазирования к релизу.
4. **RAG-001:** какой эмбеддинг-провайдер (мультиязычный, 1536-dim или смена размерности) и кто ре-эмбеддит 459 доков; до фикса — баннер в чате «ответ без цитирования ГОСТов».
5. **INFRA-003:** верифицирован ли словарь документов этапов методологом (LLM-генерация по ГОСТам — шаг спеки №16)?
6. **LLM-латентность 55–60 с** в этом окружении — таймаут клиента/модель для демо.
7. **npm-тесты кандидата 16/20** — синхронизация ugt-trajectory/ui-shell с рефакторингом.
8. Окружающие действия ревью: dev-сервер :8000 (на другой БД) остановлен и заменён тест-БД backend; фронт :3000 и бэк :8000/:8001 оставлены живыми для ручной проверки (рецепт рестарта — findings/05 §7).

---

## Roadmap приоритизации (когда решите чинить)

### Волна 0 — релиз-блокеры (до 31.08)
| ID | Что | Размер |
|---|---|---|
| RAG-001 | реальные эмбеддинги + ре-эмбеддинг + smoke-тест >0.3 | L |
| INFRA-005 / REV-03 | выравнивание прод-БД до 0026 | S (процесс) |
| REV-02 + INFRA-004 | закоммитить код бэка, выровнять структуру ветки и CI-job | S–M |
| REV-01 | синхронизировать прод-источник фронта с кандидатом | S (процесс) |
| BE-101 | auto_confirmed → published (или этапы для auto_confirmed) + тест | M |
| DEP-002 | `npm audit fix` + build/e2e | S |

### Волна 1 — качество ядра
BE-102 (guard существования проекта + запрет публикации lvl0), BE-103 (извлечение текста + фильтр scan_status), FE-101 (единый клиент с таймаутом), BE-LOGIC-003 (hash/префикс в аудите), INFRA-002 (venv 3.12), REV-05 (npm-тесты).

### Волна 2 — полировка (P3)
FE-005/007/102/103/104/105/106/107/108/109/110/111, FE-UX-003 (модал вместо prompt), BE-104/105/106, INFRA-006/007, DEP-003/004.

---

*Артефакты: спека `.scratch/code-review-130826/spec.md`, тикеты `issues/01…08`, findings `01…07`, скриншоты `screenshots/`, трекер `PROGRESS.md`. Коммиты не выполнялись (решение — за Functional Validator).*
