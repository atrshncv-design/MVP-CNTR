# 05 — Создание проекта и сохранение опросника (API)

**What to build:** Эндпоинт создания проекта (название, описание, категория, целевой УГТ): создаёт проект, добавляет создателя участником с `is_priority=true`, генерирует join-токен, возвращает id и токен. Сохранение ответов опросника по уровням (существующий эндпоинт) — с RBAC. Список своих проектов по роли/членству.

**Blocked by:** 04 — RBAC на бэкенде

**Status:** ready-for-agent

- [ ] `POST /projects` → 201 с id проекта и токеном; создатель — приоритетный участник
- [ ] Ответы опросника сохраняются и перезаписываются по уровням
- [ ] `GET /projects` — владельцу свои, участнику — где состоит, админу/менеджеру — все
