# 07 — Генерация документов (API)

**What to build:** Починить генератор: резолвер всех переменных шаблонов (включая `{{project_budget_percent_30/40}}` и `level_N_*` — без остающихся `{{...}}` в тексте); результат генерации сохраняется в список документов проекта (`project_documents`, статус `draft`, версия) с записью в аудит; валидация типов документов (tz/passport/teo) сохраняется.

**Blocked by:** 05 — Создание проекта и сохранение опросника

**Status:** ready-for-agent

- [ ] ТЗ/Паспорт/ТЭО генерируются без неразрешённых `{{...}}`
- [ ] Документ сохраняется в БД и появляется в списке документов проекта
- [ ] Генерация фиксируется в аудит-журнале
