# 20 — Схема данных нового ядра (миграция)

**What to build:** Миграция БД, которая даёт модели данных под все новые механики: словарь требований этапов УГТ (8 переходов N→N+1), привязка документов проекта к требованиям этапа, заявки на повышение УГТ с лентой статусов и историей попыток, верифицирующие документы регулирующей организации, поле «предварительный УГТ» на проекте-черновике. Плюс переименование роли: slug `ugt_expert` → `regulating_organization`, name «Эксперт УГТ» → «Регулирующая организация» (в `roles`, `user_roles`), с сохранением остальных ролей и прав.

**Blocked by:** None — can start immediately (после 19 — демо-маршрут и QA подтверждены)

**Status:** ready-for-agent

- [ ] Таблица `stage_requirements`: переход (from_level, to_level, уникальная пара), категория документа (title), описание/признаки — 8 этапов (1→2 … 8→9), заполняется seed-скриптом [PLACEHOLDER: LLM-генерация по ГОСТам + верификация методологом]
- [ ] `projects`: поле `preliminary_level` (предварительный УГТ от экспресс-оценки) или его аналог; статусы черновика (`draft` → `published`/`rejected`) — без поломки существующих значений
- [ ] `project_documents`: поле `stage_requirement_id` (FK) для привязки документа к требованию этапа
- [ ] Таблица `promotion_requests`: project_id, from_level, to_level, статус (`docs_uploaded` → `pre_evaluated` → `pending_manager` → `approved` | `rejected`), причина отклонения, оценивший менеджер, attempt_no, история попыток
- [ ] Таблица `verification_documents`: project_id, uploader_id (участник, в т.ч. регулирующая организация), ссылка на файл, comment — «документ о подтверждении УГТ» как материал для менеджера
- [ ] Миграция роли: `roles.slug`/`name` для регулирующей организации; обновление `user_roles` (роль присваивается та же, по id), RBAC-код (`projects.py:261` и др.) — без потери пользователей
- [ ] Индексы: Hash/B-Tree по новым внешним ключам и полям-фильтрам (по правилам AGENTS.md)
- [ ] Alembic-миграция + SQL-файл в `db/migrations/sql/` (как предыдущие 0009…), апгрейд на head проходит
