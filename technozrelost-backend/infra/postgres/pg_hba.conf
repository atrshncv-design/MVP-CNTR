# TYPE  DATABASE        USER            ADDRESS                 METHOD
# Сокет: только peer для OS-пользователя postgres и пароль для app/replication.
local   replication     replicator                              scram-sha-256
local   all             postgres                                peer
local   all             all                                     scram-sha-256

# Только production Compose: его БД не публикуется на host и принимает
# подключения исключительно из фиксированной сети production.
host    replication     replicator      172.30.0.0/24            scram-sha-256
host    all             all             172.30.0.0/24            scram-sha-256
